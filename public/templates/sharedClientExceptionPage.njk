{% extends "layout.njk" %}
{% block main %}

/**
 * Base exception of the generated client.
 *
 * Every client-specific runtime failure raised by the generated code
 * (serialization errors, PDA derivation failures, program errors,
 * missing extensions) is an instance of this class. Invalid caller
 * input is reported with the SPL `\InvalidArgumentException` instead.
 */
class ClientException extends \RuntimeException {}
{% endblock %}
